import React from "react";
import ReactDOM from "react-dom";
import "./style.scss";
import App from "./App";

// Render the componenet to the DOM
ReactDOM.render(<App />, document.getElementById("root"));
